# The idea is to point it at a local folder, tell it the list of boorus these images came from, and it'll do the scan, md5 hash, link up and upload etc

import click

@click.command()
def cli():
    pass