# Idea is to clear tags and metadata off posts
import click

@click.command()
def cli():
    pass

# Do a thumbnail similar check
# Do a filesize check
# Do a file type check
# Do a video length check
# Do a file size check
# Do a source check, don't merge if they contain sources from the same domain, just mark them as related